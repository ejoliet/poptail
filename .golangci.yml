version: "2"

linters:
  exclusions:
    paths:
      # Throwaway spike code (gate artifacts, kept for reference) — the
      # "golangci-lint clean" constraint in README applies to v1 code.
      - spike1
      - spike2
      - spike3
